const term = require('../../../util/term')
const { TINY_SAMPLE } = require('../../constants').OPTIONS

module.exports = class SamplesTemplate extends require('../../plugin') {
    requires() {
        console.warn(term.warn(`'cds add samples' is deprecated and will be removed in future versions. Please use 'cds add tiny-sample' instead.\n`))
        return [TINY_SAMPLE]
    }
}
