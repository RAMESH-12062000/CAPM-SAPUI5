---
modules:
 - name: {{appName}}-srv
   type: {{language}}
   path: {{& srvPath}}
   requires:
    - name: {{appName}}-redis

resources:
  - name: {{appName}}-redis
    type: org.cloudfoundry.managed-service
    parameters:
      service: redis-cache
      service-plan: default
