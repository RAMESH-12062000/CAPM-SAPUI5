const term = require('../../../util/term');

module.exports = class NotebookTemplate extends require('../../plugin') {


    async canRun() {
        console.log(term.warn('CAP Jupyter Notebooks have been replaced by Custom CAP Notebooks for VS Code!'));
        console.log(`They are now part of the CDS Editor: ${term.info('https://cap.cloud.sap/docs/tools/#add-cds-editor')}`);
        return false;
    }
}
